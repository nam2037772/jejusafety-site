# HANDOFF — 사례 증거 보강 작업 (중단 지점)

작성 2026-08-28 · 브랜치 `main` · **커밋/배포 안 함. 전부 작업트리에만 있습니다.**

---

## 1. 지금 상태 — 검증은 통과

```
node tools/check-cases.js   → 전체 10건 / 발행 9건, 오류 0, 경고 1 (아래 §4)
node tools/check-site.js    → HTML 27장, 문제 없음
node tools/build.js         → 실행됨 (HTML 전량 재생성 완료)
```

빌드 산출물까지 최신입니다. 로컬에서 바로 열어 볼 수 있는 상태입니다.

---

## 2. 끝난 변경

### 사례 008 분리 (승인 항목 1) — 완료
- `008` → **기둥 반사테이프**로 좁힘. slug 변경
  `jeju-parking-reflective-tape-delineator-post` → `jeju-parking-pillar-reflective-tape`
- `011` 신설 — **시선유도봉 + 경계석 반사 경고도색**
- 사진 `git mv` 로 이동: `008/after-04·05·06` → `011/after-01·02·03`
- **두 사례가 공유하는 사진 0장.** 008 = 주차면 기둥 / 011 = 진입 모서리 경계석

### 사진 회수 (승인 항목 2) — 부분 완료
- `008/process-01` — 배경 건물의 **기관 명패**가 찍혀 있어 상단 24% 크롭. 발주처 미확인 상태 유지
- `007/after-01·02·04` — 좌측 제3자 현수막(상호·전화번호) 제거를 위해 좌측 19% 크롭. **완료**
- 원본 백업: `%TEMP%\claude\c--Projects\c8bd3cb4-…\scratchpad\photo-backup\`
- ⏳ **미완료**: 크롭한 007 사진 3장을 `cases.js` 의 `images.after` 에 아직 넣지 않았습니다
- ⏳ **미완료**: 001(6장)·009(10장) 미사용 사진 갤러리 편입 — **손도 안 댔습니다**

### dateBasis (승인 항목 3) — 완료
- `date` + `dateBasis:'문서'|'사진'` 2필드. 사진 근거면 화면에 **「2026년 7월경」**
- 10건 전부 `date` 채워짐 → **정렬(최신순)이 실제로 작동하기 시작**
- 사진 근거는 JSON-LD `datePublished` 에 **넣지 않음** (근사값을 단정으로 만들지 않기 위해)
- 상세 페이지에 근거를 밝히는 각주 1줄 추가

### evidenceType (승인 항목 4) — 완료
- `'시공' | '납품' | '유지보수'`, 기본값 `'시공'`
- 현재 분포: 시공 7 · 유지보수 3 · **납품 0**
- 목록에 「기록 유형」 필터 칩 + 카드 배지. **실제 존재하는 유형만** 칩으로 냄(납품 0건이라 안 나옴)
- guides 는 손대지 않음 — `guide/` 로 완전히 분리된 상태 유지

### hasCase 정합성 (승인 항목 5) — 완료
- `service-copy.js` 22개 행: `hasCase: true/false` → **`caseIds: [id]`** (검증 가능한 형태로)
- 배지가 `시공사례 있음`(텍스트) → **`시공사례 보기 →`(해당 사례로 링크)**
- 발행 중인 사례가 없으면 배지 자체가 안 나옴
- `check-cases.js` 신규 규칙: 없는 id → 오류 / 전부 미발행 → 경고
- **잡아낸 실제 과장 2건**
  - `일반 배수로 그레이팅` — 발행 사례는 004(중하중)뿐 → 근거 없음, 배지 제거
  - `스텐 자바라 대문 (앵글 프레임형)` — 근거가 007(미발행)뿐 → 배지 자동 제거

### ainsafety.com (승인 항목 6) — 부분 완료
- `EXTERNAL_LINKS.shop` 을 **단일 출처로 승격** (`label` / `shortLabel` / `desc` / `url`)
- products 페이지: 품목군마다 반복되던 **「쇼핑몰에서 구매」 고스트 버튼 5개 제거**.
  상단 대표 CTA **「안전용품 온라인 구매」 1개만** 유지
- 푸터: **「안전용품 온라인몰」** 로 통일, 전 페이지 유지. **헤더에는 추가 안 함**
- JSON-LD 는 **변경 없음** — 기존 `sameAs`(#brand·#operator) + `parentOrganization` 이
  이미 사실과 정확히 맞습니다. 없는 관계를 만들지 않았습니다

---

## 3. 다음에 할 일 (우선순위 순)

1. **pages.js 의 남은 「쇼핑몰」 표기 통일** — 아직 옛 문구가 남은 줄:
   `105 · 204 · 210 · 383 · 389 · 396 · 415 · 485 · 610 · 709 · 868`
   (대표 CTA·푸터는 이미 새 문구. 본문·FAQ 문장만 남았습니다)
2. **007 크롭 사진 3장을 `cases.js` 에 반영** — `images.after` 에 추가 + `review.notes` 에
   "현수막 크롭으로 해소, 발주처 승인만 남음" 기록.
   ⚠️ **`published: false` 는 그대로 둘 것** (지시사항). `.gitignore` 의 007 줄도 그대로 둘 것
3. **001·009 미사용 사진 갤러리 편입** — 001 `after-07~12`, 009 `process` 짝수번 10장.
   같은 현장·같은 공정이므로 **새 사례로 만들지 말 것**
4. **문서 갱신**
   - `docs/CONTENT_MODEL.md` — `dateBasis` · `evidenceType` · `caseIds` 추가 반영
   - `docs/CASE_IMPORT_AUDIT.md` — 008 분리 기록 + **010 사진 3장을 "삽화"로 적은 것은 오분류**
     (실제 현장 실사임 — 감사에서 확인) 정정
   - `data/case-roles.json` — 008 이 두 사례로 갈라졌다는 주석. 재import 시 혼동 방지
5. TEST → BUILD → 검증 → 커밋 → 배포 → 실제 페이지 확인

---

## 4. 남은 경고 1건 (정상 동작)

```
[service-copy:metal-fabrication] '스텐 자바라 대문 (앵글 프레임형)' 이
가리키는 사례(7)가 모두 미발행입니다 — 표에 시공사례 링크가 나오지 않습니다
```

**고칠 것이 아닙니다.** 새 검증 규칙이 의도대로 작동한 결과입니다.
007이 공개 승인되면 자동으로 사라집니다.

---

## 5. 배포 전 판단이 필요한 것

**URL 1개가 사라집니다.** 008의 slug 를 바꿨기 때문입니다.

```
없어짐: /case/008-jeju-parking-reflective-tape-delineator-post.html
새로:   /case/008-jeju-parking-pillar-reflective-tape.html
        /case/011-jeju-parking-delineator-post-kerb-marking.html
```

이미지 3장의 주소도 `008/after-04·05·06.jpg` → `011/after-01·02·03.jpg` 로 바뀝니다.

사이트가 배포된 지 얼마 안 돼 색인 노출은 적을 것으로 보이지만,
**옛 주소에 meta-refresh 스텁을 둘지 그냥 버릴지는 결정이 필요합니다.**
GitHub Pages 라 서버 리다이렉트는 못 씁니다.

---

## 6. 손대지 않은 것 (지시대로)

- 010 사진 3장 → **신규 사례로 만들지 않음.** 현장·날짜 미상 상태 유지
- 지역 · 발주처 · 수량 · 시공일 **추정 없음**
- 007 `published: false` **유지**, `.gitignore` 의 007 줄 **유지**
- guides 구조 변경 없음 · 새 프레임워크 없음 · 대규모 리팩터링 없음

> 이 파일은 작업 재개용 메모입니다. 작업이 끝나면 지우세요.
